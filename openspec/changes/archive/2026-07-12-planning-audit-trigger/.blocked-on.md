# 착수 대기 (BLOCKED)

이 change(`planning-audit-trigger`, ⑦ 감사 실행 버튼)는 **도로로의 wireframe 후속 2단계가 끝날 때까지 착수 보류**한다. (사용자 결정 2026-07-08)

## 대기 이유
⑦의 Phase 2·3(flowforge측)이 `web/src/App.tsx`·`server/src/routes/docs.ts`를 편집하는데, 도로로의 wireframe 계열 change가 같은 파일을 HOT하게 편집 중이라 머지 충돌 위험. 순차 착수로 충돌 회피.

## 착수 조건 (아래 3개 change가 모두 archive되면 해제)
도로로 wireframe은 3단계 분리(proposal 명시):
1. `planning-wireframe-device-frame` — 렌더러 재작성 (현재 진행 중, 0/6)
2. **후속 ②** — AI 레이아웃 생성 (change 아직 미생성)
3. **후속 ③** — 위저드 승인 + 피드백 재생성 (change 아직 미생성)

**해제 조건**: 위 1·2·3이 전부 `openspec/changes/archive/`로 이동 + flowforge 활성 change에 wireframe 계열이 0개.

## 착수 시 순서 (design.md 참조)
- Phase 1(워커측, openspec-reports repo, 도로로 무충돌)은 조건과 무관하게 먼저 가능하나, worker.py=RCE 코어라 착수 전 사용자 승인 필요(비가역 게이트).
- Phase 2·3(flowforge측)은 위 해제 조건 충족 후.

## 확인 방법
```
bash openspec/changes/planning-audit-trigger/.check-ready.sh
```
